const Logo = require('../models/carLogoSchema')
let multer = require('multer')

//todo: error handler

//Create image
const create_all_default_images = () => {}
//in query params: makeId
const logo_get_by_make_id = (req, res) => {}
